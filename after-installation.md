# Things to do after installation
Whenever needed, you can get rid if unneeded versions using ```sudo apt autoremove```
Needed to install some r packages like readODS
```sudo apt-get install libxml2-dev```
